<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_widget"
    android:orientation="vertical"
    android:padding="@dimen/space_3">

    <!-- 标题栏：应用名 + 统计 -->
    <LinearLayout
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textColor="@color/widget_text_primary"
            android:textSize="@dimen/widget_text_title"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_summary"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/space_2"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/widget_text_secondary"
            android:textSize="@dimen/widget_text_small" />
    </LinearLayout>

    <!-- 分隔线 -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="@dimen/divider_height"
        android:layout_marginTop="@dimen/space_2"
        android:layout_marginBottom="@dimen/space_1"
        android:background="@color/widget_divider" />

    <!-- 任务列表 -->
    <ListView
        android:id="@+id/widget_task_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@color/widget_divider"
        android:dividerHeight="@dimen/divider_height" />

    <!-- 底部操作栏：日期 + 按钮 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_1"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/widget_date"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/widget_text_secondary"
            android:textSize="@dimen/widget_text_mini" />

        <TextView
            android:id="@+id/btn_run"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_widget_button"
            android:paddingLeft="@dimen/widget_button_padding_horizontal"
            android:paddingRight="@dimen/widget_button_padding_horizontal"
            android:paddingTop="@dimen/widget_button_padding_vertical"
            android:paddingBottom="@dimen/widget_button_padding_vertical"
            android:text="@string/widget_run"
            android:textColor="@color/widget_button_text"
            android:textSize="@dimen/widget_text_mini" />

        <TextView
            android:id="@+id/btn_refresh"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/space_2"
            android:background="@drawable/bg_widget_button"
            android:paddingLeft="@dimen/widget_button_padding_horizontal"
            android:paddingRight="@dimen/widget_button_padding_horizontal"
            android:paddingTop="@dimen/widget_button_padding_vertical"
            android:paddingBottom="@dimen/widget_button_padding_vertical"
            android:text="@string/widget_refresh"
            android:textColor="@color/widget_button_text"
            android:textSize="@dimen/widget_text_mini" />
    </LinearLayout>

</LinearLayout>
