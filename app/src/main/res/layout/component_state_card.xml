<?xml version="1.0" encoding="utf-8"?>
<!-- 统一状态卡（空态 / 错误 / 提示）：大徽标 + 标题 + 说明。
     替代原 Home 空态(bg_icon_circle)、User 空态(drawableTop)、Link 错误(bg_surface_container_rounded)三套写法。
     错误态：将 state_icon / state_title 的 tint 与 textColor 设为 ?attr/colorError 即可。 -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    style="@style/Widget.Venus.StateCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/state_icon"
            android:layout_width="@dimen/icon_badge_lg"
            android:layout_height="@dimen/icon_badge_lg"
            android:background="@drawable/bg_icon_circle"
            android:contentDescription="@null"
            android:padding="@dimen/space_3"
            android:src="@drawable/ic_pending"
            android:visibility="gone"
            app:tint="?attr/colorOnPrimaryContainer" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/state_title"
            style="@style/TextAppearance.Venus.Title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_3"
            android:gravity="center"
            android:textColor="?attr/colorOnSurface" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/state_message"
            style="@style/TextAppearance.Venus.RowSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_2"
            android:gravity="center"
            android:visibility="gone" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
